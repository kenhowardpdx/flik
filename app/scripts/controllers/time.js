'use strict';

/**
 * @ngdoc function
 * @name citrusApp.controller:TimeCtrl
 * @description
 * # TimeCtrl
 * Controller of the citrusApp
 */
angular.module('app')
  .controller('TimeCtrl', function ($scope) {
    if($scope) {
      // just a place holder
    }
  });
